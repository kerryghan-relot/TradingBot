"""Shared building blocks: signals, engine, broker, config, metrics.

Everything under ``core/`` is written once and consumed by both the
backtest side (``backtest/``) and the live side (``live/``).
"""
