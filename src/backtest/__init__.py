"""Backtesting engines.

``event_driven`` replays historical bars through ``core.engine`` — the
exact code path the live bot runs — guaranteeing backtest/live parity.
``vectorized/`` hosts the fast vectorbt research scripts.
"""
